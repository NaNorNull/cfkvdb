{
  "private": true,
  "name": "cfkvdb",
  "version": "0.5.0",
  "description": "A simple database with query language on top of Cloudflare KV",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "NaNorNull",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
